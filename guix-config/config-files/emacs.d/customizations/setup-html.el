(add-hook 'html-mode-hook (lambda () (setq indent-tabs-mode nil)))
(add-hook 'html-mode-hook (lambda () (setq sgml-basic-offset 4)))
(add-hook 'mustache-mode-hook (lambda () (setq mustache-basic-offset 4)))
